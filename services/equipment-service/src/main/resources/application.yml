server:
  port: 8082
  servlet:
    context-path: /

spring:
  application:
    name: equipment-service

  data:
    mongodb:
      host: localhost
      port: 27017
      database: gymandkhana
      username: admin
      password: password123
      authentication-database: admin

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.gymandkhana: DEBUG
    org.springframework.data.mongodb: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

---
spring:
  config:
    activate:
      on-profile: docker
  data:
    mongodb:
      host: mongodb
      port: 27017
      database: gymandkhana
      username: admin
      password: password123
      authentication-database: admin